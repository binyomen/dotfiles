#!/usr/bin/env bash
#
set -o errexit
set -o nounset
set -o pipefail
if [[ "${TRACE-0}" == "1" ]]; then
    set -o xtrace
fi

main() {
    ls -A | tr '\n' '\0' | du -hs --files0-from=- | sort -hr
}

main "$@"
