#!/usr/bin/env bash

declare percent=$(brightnessctl --machine-readable "$@" | awk -F, '{print $4;}' | sed 's/.$//')
notify-send --icon sunny --hint string:synchronous:brightness --hint int:value:$percent "Brightness  $percent%"
